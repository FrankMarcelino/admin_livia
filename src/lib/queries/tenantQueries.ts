/**
 * Tenant Queries - Compatibility Layer
 * Re-exporta as queries modulares para manter compatibilidade com imports existentes
 *
 * NOTA: Este arquivo mantém a API pública inalterada.
 * A implementação real está em src/lib/queries/tenant/
 */

export * from './tenant'
